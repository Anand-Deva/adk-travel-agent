apiVersion: v1
kind: Service
metadata:
  name: adk-service
spec:
  selector:
    app: adk-app
  ports:
    - protocol: TCP
      port: 8080       # Port you will use in the browser
      targetPort: 8000 # Port the container is listening on
  type: NodePort